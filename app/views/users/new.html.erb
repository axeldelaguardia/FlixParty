<div id="register_form">
	<%= form_with model: @user, method: :post do |f| %>
		<%= f.label :name, "Name" %>
		<%= f.text_field :name %>
		<%= f.label :email, "Email" %>
		<%= f.email_field :email %>
		<%= f.label :email, "Password" %>
		<%= f.email_field :password %>
		<%= f.label :email, "Password Confirmation" %>
		<%= f.email_field :password_confirmation %>
		<%= f.submit "Register", class: "btn btn-success"%>
	<% end %>
</div>